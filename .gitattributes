# Bundled rule snapshots are byte-verified (SHA-256). Do not normalize line endings.
resources/rules/*.txt -text
resources/rules/*.json -text
resources/rules/SNAPSHOT.md -text
build.sh text eol=lf
packaging/linux/*.sh text eol=lf
